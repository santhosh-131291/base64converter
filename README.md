# base64converter
Password in web_set_user should be in base64 format.Sometimes, basic authorization password will be provided and this converter will convert into Base64 format

Download the html file and double click it.

Type any text and it will be converted to Base64 format.
